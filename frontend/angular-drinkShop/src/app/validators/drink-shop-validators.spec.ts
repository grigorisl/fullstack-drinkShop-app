import { DrinkShopValidators } from './drink-shop-validators';

describe('DrinkShopValidators', () => {
  it('should create an instance', () => {
    expect(new DrinkShopValidators()).toBeTruthy();
  });
});
